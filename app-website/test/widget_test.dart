import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App basic smoke test', (WidgetTester tester) async {
    // Placeholder test — website tidak menggunakan counter
    expect(true, isTrue);
  });
}
