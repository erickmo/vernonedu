import 'package:flutter/material.dart';
import 'program_page.dart';

class ProgramKarirPage extends StatelessWidget {
  const ProgramKarirPage({super.key});
  @override
  Widget build(BuildContext context) => const ProgramPage(programType: 'karir');
}
