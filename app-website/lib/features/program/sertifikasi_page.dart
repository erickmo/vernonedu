import 'package:flutter/material.dart';
import 'program_page.dart';

class ProgramSertifikasiPage extends StatelessWidget {
  const ProgramSertifikasiPage({super.key});
  @override
  Widget build(BuildContext context) => const ProgramPage(programType: 'sertifikasi');
}
