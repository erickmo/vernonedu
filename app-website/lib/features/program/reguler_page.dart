import 'package:flutter/material.dart';
import 'program_page.dart';

class ProgramRegulerPage extends StatelessWidget {
  const ProgramRegulerPage({super.key});
  @override
  Widget build(BuildContext context) => const ProgramPage(programType: 'reguler');
}
