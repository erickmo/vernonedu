import 'package:flutter/material.dart';
import 'program_page.dart';

class ProgramPrivatPage extends StatelessWidget {
  const ProgramPrivatPage({super.key});
  @override
  Widget build(BuildContext context) => const ProgramPage(programType: 'privat');
}
