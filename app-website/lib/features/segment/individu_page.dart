import 'package:flutter/material.dart';
import 'segment_page.dart';

class SegmentIndividuPage extends StatelessWidget {
  const SegmentIndividuPage({super.key});
  @override
  Widget build(BuildContext context) => const SegmentPage(segment: 'individu');
}
