import 'package:flutter/material.dart';
import 'segment_page.dart';

class SegmentSekolahPage extends StatelessWidget {
  const SegmentSekolahPage({super.key});
  @override
  Widget build(BuildContext context) => const SegmentPage(segment: 'sekolah');
}
