import 'package:flutter/material.dart';
import 'segment_page.dart';

class SegmentKorporatPage extends StatelessWidget {
  const SegmentKorporatPage({super.key});
  @override
  Widget build(BuildContext context) => const SegmentPage(segment: 'korporat');
}
